import 'package:flutter/material.dart';

class Research2 extends StatelessWidget {
  const Research2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mood Trend'),
      ),
      body: const Center(
        child: Text('Research2'),
      ),
    );
  }
}
